## Context

A delivered application is not judged on its code but on what it puts the person using it through. This challenge walks the same usage scenario across every application delivered on the source challenge, step by step, and closes on an overall verdict.

## Objective

Walk the seven steps of the scenario on the exposed application, and mark each one: passed, blocked, or failed.

Step 3 decides the rest: MyCoach needs the camera, and a cross-origin iframe is denied it until the host page delegates the permission. A step blocked inside the embedded frame carries on in a tab; a camera that still does not start once in that tab is a genuine validation result.

## Expected result

- A complete walkthrough, every step marked and commented wherever it does not pass.
- A closing overall verdict saying whether the journey holds up.

## About this brief

Placeholder. This validation challenge is created by the seed, from `db_data/mykine-validation.json`. It does not exist in production and never had a brief written: the text above restates the scenario already written, and is worth fleshing out.
